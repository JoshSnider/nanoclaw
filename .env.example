

# Added by skill
ASSISTANT_HAS_OWN_NUMBER=

# Added by skill
TELEGRAM_BOT_TOKEN=

# Added by skill
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=

# Vercel skill — personal access token (generate at https://vercel.com/account/tokens)
VERCEL_TOKEN=

# Multi-tenancy
# TENANT_AUTO_CREATE=true           # Auto-create tenants for new senders (default: true)
# TENANT_ENCRYPTION_KEY=            # AES-256-GCM key for BYOK API keys (hex, 64 chars). Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# DEFAULT_RATE_LIMIT_RPM=20         # Requests per minute for shared-key tenants
# DEFAULT_RATE_LIMIT_DAILY=200      # Requests per day for shared-key tenants
