# Linear Skill

Manage Linear issues, projects, teams, cycles, and more. Tools are available as `mcp__nanoclaw__linear__*`.

Use `load_skill("linear")` to activate and register all tools.

## Tips

- Issue IDs look like `ENG-123` (team prefix + number)
- You can filter by assignee, label, state, priority, and team
